{% sw_extends '@Storefront/storefront/layout/meta.html.twig' %}

{% block layout_head_javascript_router %}
    {{ parent() }}
    <script>
        window.router = window.router || {};
        window.router['frontend.twint.waiting'] = '{{ path('frontend.twint.waiting', { pairingId: '--hash--'} ) }}';
        window.router['frontend.twint.status'] = '{{ path('frontend.twint.status', { pairingId: '--hash--'} ) }}';

        window.router['frontend.twint.express'] = '{{ path('frontend.twint.express', { paringHash: '--hash--'} ) }}';
        window.router['frontend.twint.express-checkout'] = '{{ path('frontend.twint.express-checkout' ) }}';
        window.router['frontend.twint.monitoring'] = '{{ path('frontend.twint.monitoring', { paringHash: '--hash--'} ) }}';
        window.router['frontend.checkout.line-item.add'] = '{{ path('frontend.checkout.line-item.add') }}';
        window.router['frontend.checkout.cart.page'] = '{{ path('frontend.checkout.cart.page') }}';
        window.router['frontend.checkout.finish.page'] = '{{ path('frontend.checkout.finish.page') }}';
    </script>
{% endblock %}
